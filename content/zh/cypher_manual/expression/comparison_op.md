# 比较运算符

我们通过以下表格展示 NeuG 支持的各种比较运算符。

运算符 | 描述 | 示例 | 结果
---------|-------------|---------|--------
`=` | 等于 | `1 = 1` | `true`
`<>` | 不等于 | `1 <> 2` | `true`
`<` | 小于 | `1 < 2` | `true`
`<=` | 小于或等于 | `1 <= 1` | `true`
`>` | 大于 | `3 > 2` | `true`
`>=` | 大于或等于 | `3 >= 3` | `true`

## NULL 值处理

NeuG 根据 SQL"三值逻辑"标准处理比较中的 `NULL` 值，比较结果可以是 `true`、`false` 和 `NULL` 之一。当比较操作中的任一操作数为 NULL 时，结果始终为 NULL，无论使用何种比较运算符。

下表展示了比较操作中如何处理 NULL 值：

运算符 | 示例 | 结果
---------|---------|--------
`=` | `NULL = NULL`, `NULL = 1` | `NULL`
`<>` | `NULL <> NULL`, `NULL <> 1` | `NULL`
`<` | `NULL < NULL`, `NULL < 1` | `NULL`
`<=` | `NULL <= NULL`, `NULL <= 1` | `NULL`
`>` | `NULL > NULL`, `NULL > 1` | `NULL`
`>=` | `NULL >= NULL`, `NULL >= 1` | `NULL`